# 千万字文档入库优化 —— 设计思考记录

> 记录一次从「规模疑问」到「方案落地」的设计推演过程,包括试错路径与取舍理由。

## 1. 问题起点:千万级规模,现有系统能扛住吗?

排查按两个口径分别推演(结论汇总):

| 口径 | 主要瓶颈 | 结论 |
|---|---|---|
| **单个千万字大文档**(1000 万字符,约 2.5 万块) | 串行 embedding、单次巨型 bulk、无断点续传 | 能"跑"但大概率中途失败、耗时数小时,**不可用** |
| **千万份文档的库** | 无分页列表、ES 存储/内存(约 6~10TB 磁盘 + 2TB RAM)、单消费者、单目录文件、单机限流、无对账重试 | **扛不住**,需架构级重构(ES 集群/量化、对象存储、消费并发、全局限流等) |

本次只解决**第一个口径**(单个大文档),第二个口径的架构问题记录在文末遗留问题中,不动。

## 2. 瓶颈定位:串行 embedding 是头号问题

逐环节排查入库链路后,定位到 `DocumentParseService`:

```java
// 旧代码:整个 for 循环跑在 1 个 MQ 消费者线程里
List<Chunk> chunks = new ArrayList<>(chunkTexts.size());
for (String chunkText : chunkTexts) {
    chunks.add(new Chunk(chunkText, embeddingService.embed(chunkText))); // 同步 HTTP,一次一块
}
```

数量级估算:

```
1000 万字 ÷ 400 字/块 ≈ 2.5 万块
2.5 万次串行 HTTP × 单次 200~500ms ≈ 83 分钟 ~ 3.5 小时
```

伴随问题(同一段代码连带暴露):

- **单次巨型 bulk**:2.5 万块攒成 `List<Chunk>` 一次写入 ES,约 250MB 单请求,客户端内存与 ES 堆都吃紧
- **无断点**:embedding 在第 2.4 万块失败 → 整篇 FAILED,已花的两小时全部作废,只能重传重来
- **上传上限**:`max-file-size: 50MB`,千万字(≈30MB)勉强过,再大直接被拒

## 3. 方案试错:为什么不是「裸并行」

### 试错 1:直接开多线程并行调 embed()

**被否**。理由:

- 上游 SiliconFlow 免费额度有 RPM/TPM 限制,32 线程无脑打会把 429 放大,**失败得比串行更快**
- 原代码没有任何重试逻辑,一次 429 中断 = 整篇 FAILED(异常被外层 catch 吞掉,状态落 FAILED)
- 并行度没有界,也不符合「客户端限速保护上游」的原则

结论:**并行必须配「有界 + 重试」,否则是负优化**。

### 试错 2:先批量,再并行(正确顺序)

LangChain4j 的 `EmbeddingModel.embedAll(List<TextSegment>)` 支持一次 HTTP 携带多条文本。对比:

| 方案 | 请求数 | 耗时估算 |
|---|---|---|
| 现状:串行单条 | 25,000 | 83 分钟 ~ 3.5 小时 |
| 裸并行 16 线程 | 25,000 | 受 RPM 限制,容易 429 |
| **批量(32/请求)串行** | **~780** | **~4 分钟** |
| 批量 + 4 并发 | ~780 | 1 分钟以内(受 TPM 约束) |

**批量优先**:请求数下降 30 倍,限流压力、延迟、网络开销同步下降;并行只是在这个基础上的二次加速。

### 最终方案:四件套

```
批量优先(32/请求) + 有界并行(固定 4 线程) + 重试退避(3 次,指数) + 断点续传(parsedChunks)
```

## 4. 落地设计

### 4.1 改动清单

| 文件 | 改动 |
|---|---|
| `EmbeddingService` | 新增 `embedBatch`(embedAll 批量 + 指数退避重试)与 `embedBatchAsync`(提交到固定线程池);保留单条 `embed` 给问答链路 |
| `EmbeddingProperties` / `application.yml` | 新增 `batch-size: 32`、`parallelism: 4`、`max-retries: 3` |
| `DocumentEntity` | 新增 `parsedChunks` 列(已成功写入 ES 的块数,断点) |
| `VectorIndexService` | `indexChunks` 增加 `startIndex` 偏移参数(分批写时 chunkIndex 全局连续) |
| `DocumentParseService` | 窗口化流水线:每窗口最多 `parallelism` 个批量请求在途 → 按提交顺序收集(保证 chunkIndex 与原文顺序一致)→ 每窗口一次 bulk → 落断点 |
| `DocumentController` / `index.html` | 新增 `POST /api/documents/{id}/retry` 重试端点 + 前端失败状态重试按钮 |

### 4.2 断点续传的语义

- 窗口内顺序:**先 bulk 写入 ES 成功,再落 MySQL 断点**——保证断点值 ≤ 实际已入库块数
- ES `_id = {docId}_{chunkIndex}` 固定:断点重跑时同批块幂等覆盖,不怕重复
- 状态机语义调整:`SUCCESS` / `PARSING`(另一消费者正在处理)跳过;`PENDING`(从头)与 `FAILED`(从断点或从头)允许处理
- 重试入口:失败后前端点「重试」→ 重新投递 MQ → 消费者从 `parsedChunks` 续传,已向量化的块**不再重复付费**
- 成功时 `parsedChunks` 置空,`chunkCount` 记总块数

### 4.3 并发与顺序的正确性

- 并行只发生在**窗口内**(最多 4 个批量请求同时在途),窗口间严格串行推进
- 收集结果时按**提交顺序** `join`,而不是按完成顺序——chunkIndex 必须与原文顺序一致,否则引用出处「第 N 段」会错位
- 线程池为共享 Bean 级固定池(4 线程),多个文档同时解析时全局并发度也有界,不会叠加打爆上游

## 5. 遗留问题(本次不动,已记录)

1. **上传 50MB 上限**:几千万字以上的单文档需要放开限制或对象存储直传
2. **Tika 全量读入内存**:千万字约 20MB 尚可,更大需流式解析
3. **千万份文档的架构级问题**(见第 1 节表格):无分页列表、ES 集群/向量量化、对象存储、消费者水平扩展、Redis 全局限流、对账/死信、增量更新
4. **单机 Guava 限流**:多实例部署时 QPS 限流失效(问答链路)
5. **语义缓存 O(n) 线性扫描**:200 条上限内可接受,高 QPS 下是热点 key

## 6. 面试话术(本次改动对应的考点)

- 「并行不是银弹:上游限流 + 无重试时,裸并行会把 429 放大,失败更快」
- 「先批量后并行:批量把请求数降一个数量级,并行只是二次加速;顺序不能反」
- 「断点续传的正确姿势:先落 ES 再记进度,配合固定 _id 保证幂等覆盖」
- 「窗口内并行 + 窗口间串行,既拿到并行收益,又保证 chunkIndex 顺序与引用溯源正确」
