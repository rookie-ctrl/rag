# Java 后端面试高频考点速查(演示文档)

> 这是一份用于演示的示例文档。把它上传到知识库,然后针对下面的内容提问,体验 RAG 全流程。

## 一、Redis 缓存三大问题

缓存穿透是指查询一个数据库中根本不存在的数据。由于缓存中没有,请求会直接打到数据库上;恶意攻击者可以用大量不存在的 key 打垮数据库。常见的解决方案有两种:一是布隆过滤器,在请求进入缓存之前先判断 key 是否存在,存在才继续查询;二是缓存空值,把不存在的 key 也缓存起来,设置较短的过期时间。

缓存击穿是指某一个热点 key 在过期的一瞬间,大量并发请求同时打到数据库。解决方案包括互斥锁、逻辑过期等。互斥锁方案是让只有一个线程能重建缓存,其他线程等待;逻辑过期方案是缓存永不过期,由后台线程异步刷新。

缓存雪崩是指大量 key 在同一时间集体过期,或者 Redis 服务宕机,导致请求全部落到数据库,压垮数据库。解决方案包括:给过期时间加随机值避免同时过期、Redis 集群高可用、以及服务降级限流等。

## 二、Spring 事务失效的场景

Spring 的事务是基于 AOP 动态代理实现的,只有通过代理对象调用方法时,事务注解才会生效。事务失效的常见场景包括:方法不是 public 的、同类内部方法自调用(此时走的是 this 而不是代理对象)、异常被 try-catch 吞掉、抛出的异常不是 RuntimeException 而 rollbackFor 又没有配置、数据库引擎不支持事务(如 MyISAM)、以及事务方法被 final 或 static 修饰等。

解决自调用问题可以通过 AopContext.currentProxy() 获取代理对象再调用,或者把方法拆到另一个 Bean 中。

## 三、JVM 内存结构

JVM 运行时数据区主要分为线程共享和线程私有两部分。线程共享的区域包括堆和方法区(JDK 8 之后方法区由元空间实现,使用本地内存);线程私有的区域包括程序计数器、虚拟机栈和本地方法栈。

堆内存是垃圾回收的主要区域,分为新生代和老年代。新生代又分为 Eden 区和两个 Survivor 区,默认比例是 8:1:1。对象优先在 Eden 区分配,经过多次 Minor GC 仍然存活的对象会被晋升到老年代。

垃圾收集器方面,JDK 8 默认使用 Parallel Scavenge + Parallel Old,JDK 9 之后默认使用 G1。G1 采用 Region 分区管理,能够预测停顿时间,适合大堆场景;ZGC 的目标是把停顿时间控制在 10ms 以内,JDK 15 正式发布。

## 四、Elasticsearch 原理

Elasticsearch 底层是 Apache Lucene。写入数据时,先写入内存 buffer 和 translog,refresh 之后生成段(segment)才能被检索到,这就是近实时搜索的原因。段是只读的,更新和删除通过标记删除实现,定期 merge 合并小段。

倒排索引是 ES 高效全文检索的核心:把文档中的词(term)建立到文档 id 的映射,查询时直接按词定位文档,时间复杂度与词命中数量相关,而不是与文档总数线性相关。

ES 8 之后支持向量检索:kNN 基于 HNSW 算法,这是一种多层近邻图结构,检索时从顶层粗粒度地快速逼近,再在底层细粒度搜索,把暴力扫描的 O(n) 降到了近似 O(log n)。

## 五、消息队列选型

RabbitMQ 基于 AMQP 协议,支持丰富的路由规则(交换机 + 绑定),消息可靠性高,管理界面友好,适合业务解耦、异步任务等场景,吞吐量万级。Kafka 采用日志追加的方式存储消息,支持磁盘顺序读写,吞吐量可达百万级,适合日志收集、流计算等大数据场景。RocketMQ 是阿里开源的消息中间件,支持事务消息,在电商场景应用广泛。

消息队列常见问题包括:消息重复消费(解决方案是消费端幂等,比如数据库唯一约束或状态机判断)、消息丢失(生产者确认、持久化、消费者手动确认)、消息积压(增加消费者、批量消费、降级非核心逻辑)以及顺序消息(单队列或 hash 到同一队列)。

## 六、分布式锁

用 Redis 实现分布式锁,核心命令是 SET key value NX EX seconds,NX 保证互斥,EX 防止死锁。释放锁时需要校验 value 是自己设置的(比如 UUID),防止误删别人的锁,校验和删除需要用 Lua 脚本保证原子性。更完善的方案是 Redisson 的看门狗机制,自动续期,防止业务执行时间超过锁的过期时间。

Redisson 基于 Redis 的发布订阅机制实现锁的等待唤醒,底层通过 Hash 结构支持可重入。
