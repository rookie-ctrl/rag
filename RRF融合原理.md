# RRF 融合原理 —— 学习记录

> 讨论背景:梳理混合检索时,对"BM25 与 kNN 两路结果怎么合二为一"做的系统讲解。
> 本文记录:大白话比喻、公式直觉、算例、项目代码走读、面试速答与话术。与 [[ES与kNN检索原理]] 配套阅读(那篇讲两路检索怎么搜,这篇讲结果怎么融)。

## 1. 问题:两个评委,打分标准不一样

混合检索的两路,分数完全不同尺度:

| 路 | 分数来源 | 范围 |
|---|---|---|
| BM25 | 词频统计,正数无上限 | 可能是 3.5,也可能是 87 |
| kNN | 向量相似度 | 余弦 [-1, 1] 或 ES 换算后的另一套尺度 |

同一个块,BM25 给 5.2、kNN 给 0.87——**两个分数不能相加,也不能直接比大小**。就像两个评委一个满分 100、一个满分 5,谁分高听谁的不公平。

**RRF(Reciprocal Rank Fusion,倒数排名融合)的思路:分数全扔掉,只看名次。**

每个块在两条名单里各有一个排名,按排名换算成分数再相加:

```
块得分 = 1/(60 + BM25名次) + 1/(60 + kNN名次)   (只出现一次就只加一项)
```

## 2. 为什么是"倒数排名"这个形状

用 k=60 画出曲线:

| 名次 | 1 | 2 | 3 | 10 | 50 |
|---|---|---|---|---|---|
| 1/(60+名次) | 0.01639 | 0.01613 | 0.01587 | 0.01429 | 0.00909 |

两个直觉:

- **前排差距小、沉底差距大**:第 1 名和第 2 名只差 0.00026,第 10 名和第 50 名差 0.005——"进了前排都有机会,沉底了就沉底了";
- **k=60 是平滑系数**:没有它(即 k=0),第 1 名贡献 1.0、第 2 名 0.5、第 3 名 0.33,第一名碾压一切;60 让曲线温和。取值来自 RRF 原始论文(Cormack et al., SIGIR 2009),是业界默认值。

**关键性质:"两边都出现"的价值远大于"单边第一"**——见下节算例。

## 3. 算例:谁最后赢?

假设某次检索,BM25 名单 `[A, B, C, ...]`,kNN 名单 `[B, E, A, ...]`:

| 块 | 换算 | RRF 得分 |
|---|---|---|
| B | 1/(60+2) + 1/(60+1) | **0.03252** ✅ |
| A | 1/(60+1) + 1/(60+3) | 0.03226 |
| E | 1/(60+2) | 0.01613 |
| C | 1/(60+3) | 0.01587 |

两个观察:

1. **B 赢了 A**,尽管 A 在 BM25 里是第一名——B 是"两路评委都认可"的块;
2. 只在一条名单里的 E、C,**即使排进前三,得分也只有双上榜块的一半**。

更极端的例子:某块在两条名单里都排第 7 → 2/67 ≈ 0.02985,依然赢过"单边第一名"(0.01639)。

**这直接解释了项目里每路召回 8 条(topK×2)而不是 4 条**(`HybridSearchService.java:34`):融合会重新洗牌,中后段名次但"双上榜"的块可能反超,召回太少会漏掉它们。

## 4. 项目代码走读

### 4.1 融合主流程(`HybridSearchService.java:44-51`)

```java
int rank = 1;
for (HitRecord hit : bm25Hits) {
    accumulate(hit, rank++, true, ...);   // 每条贡献 1/(60+rank),按 id 累加
}
rank = 1;
for (HitRecord hit : knnHits) {           // kNN 名单名次从 1 重新数
    accumulate(hit, rank++, false, ...);
}
```

### 4.2 累加逻辑(`accumulate()`,`HybridSearchService.java:65-89`)

```java
double contribution = 1.0 / (RRF_K + rank);            // RRF_K = 60
rrfScores.merge(hit.id(), contribution, Double::sum);  // 同一个块两路都上榜 → 相加
```

**融合键是 ES 文档 id**(`docId_chunkIndex`):同一个块在两路命中时分数自然叠加;只在一路命中的块保留单边分数,不丢。

### 4.3 融合之后的两道闸(排序 vs 准入)

```java
.filter(f -> f.similarity() >= 0.5)   // ① 余弦阈值:准入闸
.sorted(按 rrfScore 降序)              // ② RRF 分排序
.limit(4)                              // ③ 取 topK
```

必须能讲清楚的分工:**RRF 分数只有"相对排名"的意义,没有"绝对相关性"的意义**——0.03 算不算相关?答不上来。所以:

- **RRF 管排序**(谁在前谁在后);
- **余弦相似度管准入**(0.5 以下的直接拦掉,"两边都上榜但语义其实不相关"的块被过滤)。

两道闸各管各的事,这也是 README 里"相似度阈值过滤在应用层做"的原因。

## 5. 面试速答(典型追问)

| 追问 | 答 |
|---|---|
| 为什么不把两路分数归一化后相加? | 归一化方案(min-max、z-score)对分数分布敏感:BM25 无界、kNN 有界,分布差别大;分数只在本路内部可比,跨路比没有意义。名次是通用的"货币" |
| 为什么叫"倒数排名融合"? | 得分是名次的倒数 1/(k+rank),即 Reciprocal Rank Fusion |
| k=60 哪来的? | RRF 原始论文(Cormack 2009)默认值,起平滑作用:k=0 时第一名碾压一切,k 越大曲线越平 |
| RRF 分数能当阈值用吗? | 不能,它只表达相对顺序;项目里阈值过滤用的是余弦相似度(应用层精确计算) |
| 缺点是什么? | 丢弃分数大小信息:第一名 0.99 和第一名 0.51 贡献一样;单条名单内的微弱优势被抹平。这是"稳健"的代价 |
| 有别的融合方案吗? | 加权 RRF(给更信任的一路乘权重)、分数归一化加权和、Learning to Rank(用点击数据训练,重)。演示用标准 RRF 最简单最稳 |
| ES 能自己做 RRF 吗? | 能,ES 8.8+ 内置 `rank: {rrf: {}}`,一次搜索多路 retriever + 融合;项目在应用层实现是为了吃透原理,迁移成本低 |

## 6. 面试话术(一句话版)

> "两路检索的分数不可比——BM25 无界、kNN 有界,所以我用 RRF 只取排名、不取分数:每个块按 1/(60+名次) 在两路累加,双上榜的块自然胜出,免去归一化问题;融合只解决排序,绝对相关性交给后续的余弦阈值(0.5)兜底,两道闸各管各的。"

## 7. 与项目其他部分的关系

- 两路检索怎么搜:[[ES与kNN检索原理]](BM25 走倒排索引,kNN 走 HNSW 近邻图)
- 融合后的下游:ChatService 拼 Prompt、阈值过滤防幻觉(见 README 面试要点)
- 同构思路:Redis 语义缓存优化方案 ③ 的 RediSearch 也是"检索下沉到存储层"([[Redis语义缓存-设计反思]])
