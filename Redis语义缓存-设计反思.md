# Redis 语义缓存 —— 设计反思记录

> 讨论背景:梳理项目里 Redis 的两个用途后,对语义缓存的实现做了一次扩展性反思。
> 本文记录:现状实现、三个问题、真实成本估算、优化阶梯、动手时机,以及如何把这件事讲明白(面试向)。

## 1. Redis 在项目里的两个角色(上下文)

| 用途 | 位置 | 说明 |
|---|---|---|
| 语义缓存 | `SemanticCacheService`,key `rag:qa:cache`(List) | 相似问题直接返回历史答案,0 token 消耗 |
| 每日 token 预算 | `RateLimitService`,key `rag:stats:tokens:{date}` | INCR 计数,超预算拒绝,跨实例共享 |

容易误会的边界:QPS 限流是 **Guava 进程内**的(不是 Redis,多实例下失效);统计页面的 token 数读的是 **MySQL**(`QaRecord`);Redis 里**没有**检索结果缓存和向量存储(向量只在 ES)。

## 2. 语义缓存的现状实现(代码走读)

查询 `lookup()`(`SemanticCacheService.java:46`),每次提问都执行三步:

```
① range(KEY, 0, -1)        把缓存里全部条目(≤200 条)拉回 Java
② 逐条 JSON 反序列化        200 次 readValue
③ 逐条算余弦相似度取最高     ≥ 0.92 命中,否则 miss
```

写入 `put()`(`SemanticCacheService.java:80`):大模型回答完成后 `LPUSH` 队头 + `LTRIM 0 199` 截断。

**当时为什么这么写**:实现最简单——一个 List + 一个 for 循环,没有额外组件依赖。这是刻意取舍,不是疏漏。

## 3. 反思:三个问题 + 真实成本

### 3.1 每次请求的真实成本

| 成本项 | 估算 | 结论 |
|---|---|---|
| 余弦计算 | 200 × 1024 ≈ 20 万次乘加 ≈ 0.1ms | 可忽略,CPU 毛毛雨 |
| JSON 反序列化 | 200 次 × 每条 1~3μs | 毫秒级,还行 |
| **网络传输** | **~3MB/次** | **大头**:向量按 JSON 十进制文本存,一条 15KB+,200 条 ≈ 3MB;5 QPS 时 Redis 带宽 ≈ 15MB/s 全耗在这 |

核心矛盾不是"算得慢",而是 **"为了找一条最像的,把整个缓存搬过来"**。

### 3.2 问题一:O(n) 全量扫描 → 命中率天花板

- 查询成本随条目数**线性增长**,而 `max-size` 就是条数;
- 想提高命中率 → 调大 `max-size` → 每次搬运量同步膨胀(2000 条 ≈ 30MB/次,不可用);
- **结论:当前实现把缓存容量上限"焊死"在 200,命中率也跟着被卡死。**

### 3.3 问题二:单 key 热点

所有实例、所有请求打同一个 key。Redis 集群下它只会落在**一个分片**上,其他分片空转,读写全挤这一片。

### 3.4 问题三:伪 LRU

`LTRIM` 淘汰的是"**写入时间最久**"的条目,不是"**最久没人用**"的。天天被命中的热门答案因为存得早,反而先被挤掉——真正的 LRU/LFU 不会这么干。

### 3.5 核心洞察

> **"复杂度被容量上限买断":用 LTRIM 把 n 硬约束在 200,换来实现最简单;代价是扩展性天花板也在 200。**

这是演示规模下成立的工程取舍,面试时主动讲出这个取舍,比直接甩优化方案更有说服力。

## 4. 优化阶梯(从轻到重,条件触发时按序评估)

| # | 方案 | 做法 | 解决什么 | 代价 |
|---|---|---|---|---|
| ① | 向量二进制化 | 存 float32 byte[]/base64,不再 JSON 文本 | 传输量降 ~4 倍 | 小,改序列化 |
| ② | 两段式查找 | 轻量 List 只存 `{question, embedding}`,命中后再取答案 | 每次只搬向量 | 小,拆两个 key |
| ③ | **RediSearch 向量检索(正解)** | 换 Redis Stack 镜像,embedding 建 HNSW 索引,`FT.SEARCH KNN` 在 Redis 端做近邻 | O(n)→O(log n),容量上限打开,热 key 消失 | 中,换镜像 + 改代码 |
| ④ | 本地 L1 缓存 | Caffeine 进程内缓存高频"向量→答案",Redis 做 L2 | 重复查询不出进程 | 中,多实例一致性要想 |
| ⑤ | 真 LRU | ZSet + 命中 `ZINCRBY`,按访问权重淘汰 | 热门答案不被误删 | 小,收益不如 ③ |

**③ 的思路与 ES 端 kNN 完全同构**:本项目 ES 里已经是 dense_vector + kNN 检索,Redis 端只是同一个思路的缩小版——面试时这一点可以直接讲。

## 5. 何时动手(判断指标)

满足一条就值得升级,否则**不动是对的**(提前优化不值当):

1. 想把 `max-size` 从 200 调到几千以上抬命中率 → O(n) 立刻爆炸,必须上 ③;
2. 监控信号:`redis-cli --bigkeys` 该 key 占用异常 / p99 延迟被缓存查询拖累;
3. 多实例部署、真实流量,单 key 带宽吃紧。

## 6. 大白话版(面试讲解用)

书架比喻:

> 现在找"和我手里这本书最像的书",是把**整个书架全抱下来一本本翻着比**;书架小(200 本)没问题。
> 书架变大就抱不动了——正解是给书架**建目录索引,让管理员(Redis)自己找**,只把最像的那本递给你。

三个毛病对应:书架越大搬得越累(O(n))、几千人挤一个电梯口(单 key 热点)、"删存得最久的而不是最久没人看的"(伪 LRU)。

## 7. 面试话术(一句话版)

> "语义缓存我用了全量扫描 + 余弦,因为 LTRIM 把 n 硬约束在 200,复杂度被容量上限买断了,实现最简单。但它的扩展性天花板也在 200——要提升命中率就得放大容量,那时候 O(n) 会先爆炸,正解是把向量检索下沉到 Redis(RediSearch HNSW),和我在 ES 端做 kNN 是同一个思路。"

## 8. 行动项

- [ ] **暂不重构语义缓存**:当前规模(200 条、5 QPS)实现成立;触发第 5 节条件时按第 4 节阶梯实施,优先方案 ③
- [ ] **小修复(一行)**:每日 token 预算 key `rag:stats:tokens:{date}` 加 TTL(建议 48h),避免每天永久遗留一个 key
- [x] 本次讨论已记录(本文档),并与 [[千万字文档入库优化-设计思考]] 第 5 节遗留问题呼应
