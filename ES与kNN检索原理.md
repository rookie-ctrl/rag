# ES 与 kNN 检索原理 —— 学习记录

> 讨论背景:梳理项目检索链路时,对"向量检索(kNN)到底是怎么在 ES 里工作的"做的一次系统讲解。
> 本文记录:大白话比喻、算法直觉、项目代码走读、面试速答与话术。术语先给比喻再给名字。

## 1. 两个角色

**ES(Elasticsearch)**:搜索引擎,底层是 Lucene 倒排索引,天生擅长"关键词 → 文档"。在项目里的角色是**知识库的检索大脑**——所有分块都存在 `kb_chunks` 索引,提问时由它把最相关的块捞出来。

**kNN(k-Nearest Neighbors,k 近邻)**:一句话——**在空间里找离你最近的 k 个点**。用在检索上:把问题变成一个"点",在 ES 存的所有"块的点"里找离它最近的几个。

## 2. 向量检索的直觉:把语义变成坐标

Embedding 模型(BGE-M3)把一段文字变成一个 **1024 维向量** = "这段文字在语义空间里的坐标"。规则:**意思越接近,坐标靠得越近**。

1024 维画不出来,2 维示意:

```
        ● 缓存穿透怎么办            ● 什么是缓存穿透
                     \              /
                      \            /
                       ● 击穿、穿透、雪崩的区别

     ● Spring 事务什么时候失效        ● 索引失效的场景
```

"靠得近"用**余弦相似度**衡量:看两个坐标方向的夹角,夹角越小越像(只看方向、不看长度,适合比语义)。

**kNN 检索 = 把问题也变成坐标 → 在 ES 里找离它最近的 k 个块的坐标。**

### kNN vs BM25:本质区别

| | 怎么理解"相关" | 换个问法 |
|---|---|---|
| BM25 | 字面匹配:有没有出现这些词 | "什么是缓存穿透"里没有"怎么解决",命中差 |
| kNN | 语义匹配:意思像不像 | 没有共同关键词也能命中 |

这就是为什么项目要做**混合检索**:BM25 懂专有名词与精确匹配,kNN 懂同义改写,互补。两路结果如何合二为一,见 [[RRF融合原理]]。

## 3. 难点:几百万个坐标,怎么"快"找最近的 k 个?

**暴力做法**:查询向量与每个块向量逐个算距离。5 亿块 = 5 亿次计算,一次检索几十秒,不可用。

**ES 的解法:HNSW**(Hierarchical Navigable Small World,分层可导航小世界图),属于 **ANN(近似最近邻)**——关键词"近似":不保证找到绝对最近的,用极小精度损失换几百倍速度。

### HNSW 比喻:分层地图导航

```
第 2 层(世界地图):  ●——————————●        点少、跳得远
第 1 层(城市地图):  ●——●———●————●      范围缩小
第 0 层(小区地图):  ●─●─●─●─●─●─●─●    点密、精查
```

- 顶层点少间隔大 → 先"坐飞机"快速跳到目标区域
- 每往下一层点更密 → "打车到小区"、"走到门口"
- 从顶层一路往下跳,最后在底层找到最近邻

每一层是一张**近邻图**(朋友网络):每个点连着几个离它最近的朋友,搜索时顺着朋友关系一路走,不需要和所有人比。

### 两个代价(面试常问)

1. 图是**内存结构**(规模推演:5 亿向量 ≈ 2TB RAM,见 [[千万字文档入库优化-设计思考]]);
2. 近似搜索**可能漏**个别真最近邻 → 用 `numCandidates` 调节精度。

## 4. 项目代码走读

### 4.1 检索:`VectorIndexService.searchKnn()`(`VectorIndexService.java:97`)

```java
q.knn(k -> k
        .field("embedding")      // 在哪个字段上找最近邻
        .queryVector(...)        // 问题的 1024 维向量
        .k(8)                    // 返回 8 个最近邻(topK 4 × 2,给 RRF 融合留冗余)
        .numCandidates(100))     // HNSW 内部考察 100 个候选,从里面挑最近的 8 个
```

**k 与 numCandidates 的关系(最易搞混)**:在 100 个候选里挑出 8 个返回。numCandidates 越大越准、越慢,是"召回率 vs 速度"的旋钮。

### 4.2 建索引:`ensureIndex()`(`VectorIndexService.java:48`)

```java
.properties("embedding", p -> p.denseVector(dv -> dv
        .dims(1024)              // 向量维度,与 embedding 模型输出强一致
        .similarity("cosine")))  // 用余弦距离找最近邻
```

- `dense_vector`:声明向量字段(区别于普通文本/数字)
- `similarity("cosine")`:BGE-M3 输出已归一化,余弦只看方向——语义比较就选它;归一化后余弦与点积等价

### 4.3 一个索引服务两路检索(架构意义)

`embedding` 字段(dense_vector)给 kNN,`content` 字段(text 类型、倒排索引)给 BM25——**混合检索不需要装第二个组件**,删除文档时 `deleteByDocId` 一次清理两路数据。

### 4.4 与 Redis 语义缓存的联系

[[Redis语义缓存-设计反思]] 中优化方案 ③(RediSearch 向量检索)内部用的 HNSW 与 ES 这套**是同一种算法**——向量检索下沉到存储层,是 ES 和 Redis 共用的同一思路。

## 5. 面试速答(典型追问)

| 追问 | 答 |
|---|---|
| 为什么不用 MySQL LIKE 全文检索? | 词面匹配不懂语义;问法里没有"击穿"两个字,kNN 照样命中 |
| 为什么不单独装 Milvus 向量库? | 一个 ES 索引同时服务 BM25 + kNN 两路,免去双组件数据同步;ES 8 原生支持 dense_vector;演示规模够用。千万级再考虑专用向量库(内存/量化) |
| kNN 检索是精确的吗? | 不是,HNSW 是近似(ANN),可能漏掉个别最近邻;用 numCandidates 调召回率,召回与速度是权衡 |
| 相似度为什么用余弦而不是欧氏距离? | 语义看方向不看长度;向量已归一化时余弦等价于点积 |
| 维度为什么是 1024? | BGE-M3 原生维度;维度高表达力强,但存储计算更贵,过高还会"维度灾难"(距离区分度下降) |

## 6. 面试话术(一句话版)

> "我把 ES 当向量库用:embedding 字段建 dense_vector + cosine 索引,检索时问题向量走 kNN;底层 HNSW 是分层近邻图,用近似换速度,numCandidates 调节召回;同一索引的 content 字段走 BM25,两路 RRF 融合——一个组件搞定混合检索,不用再部署专用向量库。"
